services:
  backend:
    build: ./backend
    image: theft-backend:dev
    container_name: theft-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

# frontend service: planned Next.js 15 rebuild. Block kept as reference
  # for port mapping and depends_on wiring.
  # frontend:
  #   build:
  #     context: ./frontend
  #     args:
  #       NEXT_PUBLIC_API_URL: http://localhost:8000
  #   image: theft-frontend:dev
  #   container_name: theft-frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
  #   restart: unless-stopped