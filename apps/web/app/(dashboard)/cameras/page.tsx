export const dynamic = "force-dynamic"

export default function CamerasPage() {
  return (
    <section className="flex flex-col gap-2">
      <h1 className="font-medium text-lg">cameras</h1>
      <p className="text-muted-foreground text-sm">the camera grid arrives with its own scope</p>
    </section>
  )
}
