# Copy to terraform.tfvars to override defaults. Every variable has a
# default in variables.tf, so this file is documentation only.
# terraform.tfvars is gitignored.

environment = "dev"
project     = "theft-detection"
location    = "spaincentral"
owner       = "nizar"
