services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend/app:/app/app
      - ./ai-model/outputs/snapshots:/app/ai-model/outputs/snapshots:ro
    environment:
      - DEBUG=True